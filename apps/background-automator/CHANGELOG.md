# 변경 기록

배포한 빌드마다 **바꾼 것 · 왜 · 재보니 어땠나**를 남긴다.

무엇을 바꿨는지는 `git log`에 이미 있다. 여기 적을 값은 **결과**다 — 특히
해봤는데 안 돼서 되돌린 것. 그게 안 남으면 몇 주 뒤에 같은 걸 또 시도한다.

제목의 아이디는 `analyze-logs.py`의 빌드별 비교표에 나오는 값과 같다.
빌드 도장은 `72ddd3a-0726T1615`부터 찍혔고, 그 이전은 커밋 해시로만 적는다.

결과 표시: ✅ 측정으로 확인 · ❌ 측정으로 기각 · ⏳ 아직 측정 못 함

---

## 다시 시도하지 마라 (실측으로 기각됨)

| 시도 | 왜 안 되나 | 근거 |
|---|---|---|
| OCR 언어 보정 끄기 | 신뢰도는 올랐지만(0.50→1.00) `장면 넘기기`를 `장면 넘기기_`로 읽는다. 이 글자는 컷신 중 오클릭을 막는 **금지어 가드**라 어긋나면 컷신을 못 넘긴다 | 2026-07-26 프로브 실측 |
| 캡처 스트림 리팩터 | 107ms만 줄어든다. 진짜 비용은 다른 데 있었다 | 착수 전 계측이 반증 |
| 자동 전투 시작 버튼 클릭 | 그 버튼은 **토글**이다. 앱의 클릭이 3,956ms 걸려 늘 늦게 눌렸고, 이미 시작된 전투를 **멈췄다** | 2026-07-25, 규칙 제거 |
| 정지 감지 60초 | 정상 파밍에도 던전 입장~보스 컷신까지 **최대 77초** 조용하다. 오탐 | 132런 실측 → 150초로 |
| 임무 선택 화면 전용 규칙(`mission_selection`) | 입장 화면과 겹쳐 서로를 밀어냈다 | 2026-07-26, 규칙 제거 |
| 파밍 중 성능 측정 | Vision은 맥에 하나뿐이라 앱과 경쟁한다. 조용할 때 499ms → OCR 테스트 중 1,818ms → 컴파일 중 2,714ms | 5분 버킷 시계열 |

---

## eb583b8-0729T1246 — 07/29 12:46

**바꾼 것** 클릭 없이 끝난 바퀴마다 이유를 남긴다(`NoActionReason` 9종). 진단 파일이 인식 텍스트를 자르지 않는다.

**왜** 멈출 때마다 추측으로 진단해야 했다. 07/29에 `다시 하기`를 후보로 잡은 채 150초를 서 있었는데 안내는 "누를 버튼을 못 찾았습니다"였다. 버튼은 내내 찾고 있었다 — **찾고도 못 누른 것**이다.

원인을 못 짚은 이유가 둘이다.

| 문제 | 결과 |
|---|---|
| 클릭 없이 끝난 바퀴가 전부 그냥 `.noAction` | '누를 게 없었다'와 '찾고도 못 눌렀다'가 구분 안 됨 |
| 재확인 게이트가 조건 6개를 한 `guard`로 묶음 | 어느 조건에서 떨어졌는지 알 수 없음 |
| 진단 파일이 텍스트를 24개에서 자름 | 하필 화면 아래 버튼이 빠져 "버튼을 못 읽었나"로 오진 |

**안내가 정반대였다** 후보를 쥐고도 못 누른 경우엔 게임 화면을 아무리 봐도 멀쩡하다. 이제 `heldACandidate`로 갈라서, 그때는 "앱 상태를 확인하세요"라고 한다.

**24개 상한을 없앤 근거** 항목 하나가 107바이트다. 40개여도 4.2KB고, 이 파일은 매 관찰마다 덮어쓰므로 쌓이지도 않는다. 아껴봐야 1.7KB인데 그것 때문에 진단이 틀렸다.

**안 한 것** 클릭 실패 시 재시도. 왜 못 눌렀는지가 아직 확정되지 않았다. 지금 붙이면 추측으로 고치는 것이고, 안 나아도 이유를 모른다. 다음 멈춤 한 번이면 사유가 남는다.

**결과** ⏳ 다음 멈춤 때 `status.json`의 `noActionReason` 확인.

---

## 3e9102e-0728T1331 — 07/28 13:31

**바꾼 것** 전리품에 수량을 함께 남긴다. 아이콘에 붙는 뱃지 숫자를 이름과 짝지어 `quantities`에 적는다.

**왜** "영혼석 몇 개 나왔어"에 답할 수 없었다. 지금까지는 '나왔다/안 나왔다'만 세고, 뱃지는 `isQuantityBadge`가 통째로 버렸다.

**어떻게 갈랐나** 한 칸에 숫자가 두 종류 붙는다 — 우하단 **수량**(`180`·`25`)과, 장비에만 붙는 위쪽 **전투력**(`1151`·`3860`). 가로로는 둘 다 이름과 100% 겹쳐 구분이 안 된다. 갈리는 건 세로 거리뿐이다.

| 라벨 | 이름과의 거리 | 글자 높이 대비 |
|---|---|---|
| 수량 뱃지 | 46px | 2.3배 |
| 장비 전투력 | 90px | 4.5배 |

3배를 경계로 삼았다. 좌표를 상수로 박지 않고 글자 높이로 재므로 창 크기가 달라도 같은 기준이 선다.

**안 한 것** 뱃지가 없는 아이템(갱신권·패션 티켓 조각 보물 상자)은 키 자체를 비웠다. '뱃지 없음 = 1개'는 성립하지 않는다 — 실제로 숫자가 아예 없는 아이템이 있다.

**결과** ⏳ 옛 기록 550판에는 수량 칸이 없다(옵셔널이라 그대로 읽힌다). 이 빌드부터 쌓인다.

---

## de28b2a-0726T2015 — 07/26 20:15

**바꾼 것** 규칙 이름 `deselect_double_loot` → `deselect_coin`. 캡처 21장 × 옵션 3가지 = 63조합의 시나리오 표를 실제 OCR로 찍어 내는 테스트 추가.

**왜** 이름이 하는 일과 어긋났다. 더블 루팅을 끄는 건 `turn_off_double_loot`이고, 저건 은동전 던전에서 '쓰기 끔'일 때 **임무를 해제**하는 규칙이다. 그리고 시나리오를 말로 정리하다 두 번 틀려서, 표를 손으로 쓰지 않기로 했다.

**결과** ✅ 63조합 전부 후보가 정확히 1개. 겹치는 조합이 있으면 테스트가 실패한다 — 겹치면 모호성으로 멈추기 때문이다.

**덤으로 확인된 것** 표에서 유일하게 후보가 없는 `landscape-result-early`가, 던전 헤더가 전리품 수집 구간을 지나가는 바로 그 프레임이었다(`룬다 1층 2구역` y=0.505, `순수 전투 시간` y=0.544). "애니메이션 중 헤더가 구간을 지난다"는 건 추측이었는데 픽스처가 증명했다.

---

## 655991c-0726T1850 — 07/26 18:50

**바꾼 것** 재화가 바닥나도 '쓰기'를 켠 채로 멈추지 않는 것을 옵션 켬/끔 두 경우 다 테스트로 고정. 문서 정정.

**결과** ✅ 둘 다 해제로 빠진다. 다만 **이유가 서로 다르고, 우연에 가까웠다.**
- 공물 0 → `선택을 해제하면...` 안내문이 뜬다. `enter_with_tribute`가 그 문구를 금지어로 갖고 있어 물러난다(의도한 설계).
- 은동전 부족 → 더블 루팅 카드가 **통째로 사라져** `도전에 성공하면...`이 없어진다. `enter_with_coin`은 그 문구를 필수로 요구하므로 저절로 물러난다(의도하지 않은 부수 효과).

**정정** "더블 루팅 카드는 은동전이 모자라 회색일 때도 남는다"고 적었던 건 **은동전 10개**일 때 얘기였다. 7개처럼 임무값에도 모자라면 카드가 사라진다.

---

## 620b75a-0726T1839 — 07/26 18:39

**바꾼 것** 메뉴의 '오늘 사이클'에 돌린 시간을 붙였다(`41회 · 1시간 22분`). 인접 사이클 간격을 더하되 5분 넘는 공백은 뺀다.

**왜** 판 수만으로는 얼마나 붙잡고 있었는지 모른다. 옆의 '가동 시간'은 이번에 시작한 뒤부터라 껐다 켜면 0이 된다.

**결과** ✅ `analyze-logs.py`의 날짜별 가동 시간도 같은 기준으로 맞췄다. 전에는 그날 첫 판부터 마지막 판까지의 폭이라 자리를 비운 시간이 통째로 들어갔다 — 07/25가 **9.2시간 → 4.8시간**으로 정정됐고, 판당 소요도 그만큼 부풀어 있었다.

---

## 15db131-0726T1834 — 07/26 18:34

**바꾼 것** 공물 던전(페카 고분) 지원. `deselect_tribute` / `enter_with_tribute` 규칙과 메뉴 '공물 쓰기'(기본 끔). `CycleRecord`에 입장 방식(`coin`/`tribute`/`free`) 기록. 전리품에 섞이던 것 3건 제거.

**왜** 공물이 남아 있으면 입장 화면에서 멈췄다. 페카에는 더블 루팅 카드가 없어 `도전에 성공하면 임무 전리품이 두 배가 됩니다.`가 뜨지 않는데, 은동전 규칙 전부가 그 문구를 필수 조건으로 삼고 있었다.

**결과** ✅ 실캡처 9장 골든 테스트 통과 — 공물 있음/없음 × 선택/해제 네 가지 입장 화면, 결과 화면 세 가지, 확인 다이얼로그. 던전 이름 통합으로 페카가 27+8+1+1판 → **41판 한 덩어리**가 됐다.

**배운 것 두 가지.**
`선택을 해제하면 임무 없이 입장할 수 있습니다.`는 은동전 전용 문구가 아니라 **재화가 모자랄 때 뜨는 공용 안내**다. 그래서 재화가 떨어지면 기존 규칙이 받아 스스로 빠져나왔고, 그 덕에 버그가 오래 안 보였다.
룬다가 안 멈춘 이유는 더블 루팅 카드가 **은동전이 모자라 회색일 때도 화면에 남아** 있어서다. 우연히 걸려 있던 조건이었다.

**가드레일이 4건 잡았다** — `AutomationScene` case 누락, 규칙 화이트리스트, 신뢰도 하한, 씬 매핑. 전부 실제로 배선이 빠진 자리였고, 테스트가 없었으면 런타임에 규칙이 조용히 무시됐을 것이다.

---

## 72ddd3a-0726T1615 — 07/26 16:15

**바꾼 것** 사이클·클릭 기록에 빌드 아이디를 찍는다. `builds.jsonl`에 빌드별 첫 실행 시각과 변경 내역. `analyze-logs.py`에 빌드별 비교표.

**왜** 배포가 성능을 바꿨는지 볼 방법이 시각으로 로그를 자르는 것뿐이었고, 두 군데서 어긋났다. 빌드해도 앱을 다시 켜지 않으면 옛 바이너리가 계속 돈다. 배포 시각이 로그에 없어 `ps -o etime`으로 역산해야 했다.

**결과** 계측 자체를 더한 변경이라 이 빌드가 성능을 바꾸지는 않는다. 다만 이 빌드는 아래 `5ce514d`(클릭당 OCR 3회→2회)를 포함하므로, 표에 찍히는 숫자가 곧 그 변경의 성적표다.

⏳ **잠정(7판, 16:20~16:27 조용한 구간)** — 판당 106초 → 100초, 보상 읽기 95% → 100%. 표본 20판을 넘겨야 확정한다.

**부수 확인** 이 빌드로 비교표가 처음 제 일을 했다. 같은 구간에서 화면 읽기가 컴파일 중 2,035ms → 손 뗀 뒤 **429ms**로 갈렸고, 그동안 "느리다"고 느낀 원인이 새 빌드가 아니라 옆에서 돌던 빌드 작업이었음이 숫자로 확정됐다.

---

## 5ce514d — 07/26 15:57 (도장 이전)

**바꾼 것** 클릭당 OCR 3회 → 2회(`stableObservationCount` 2 → 1). 결과 화면 관찰 간격 2초 → 1.2초.

**왜** 후보를 서로 다른 사이클에서 두 번 봐야 누르게 돼 있어 클릭마다 550ms가 더 들었다. 판당 6클릭이면 3.3초. 누르기 직전 재확인이 남아 있어 안전은 그쪽이 지킨다.

**결과** ⏳ **잠정 — 위 `72ddd3a` 항목의 숫자가 이 변경의 성적표다.** 이 빌드 자체로는 측정하지 못했다. 배포 직후부터 내가 계속 컴파일·테스트를 돌려 깨끗한 구간이 1분도 없었기 때문이다.

**배운 것** 배포하자마자 성능을 판단하려 들지 마라. 손을 떼고 20판(약 35분)을 둬야 숫자가 나온다.

---

## 98bca4f · a81ec70 — 07/26 15:33~15:49 (도장 이전)

**바꾼 것** 코드 리뷰 지적 4건(멈춤 1회에 14줄씩 쌓이던 로그, 클릭 안 한 사이클도 세던 침묵 감지 구멍, 안전 검증 밖에 있던 은동전 규칙, 무테스트 분기) + 단순화.

**결과** ✅ 동작 유지. 다만 단순화 에이전트가 `didAct` 조건에서 `.clickOutcomeUncertain`을 조용히 빼놓고 "동등하다"고 보고했다 — 되살렸다. **에이전트의 '동등' 주장은 그대로 믿지 마라.**

---

## 972c7b9 · ac06fb0 — 07/26 14:53~15:03 (도장 이전)

**바꾼 것** 전리품 이름에서 수량 뱃지(`255.3만`) 제거, 아이콘 밑 두 줄 이름 잇기. 쌓인 로그도 같은 규칙으로 정리.

**왜** 단위 `만`·`천`·`억`이 한글이라 한글 필터를 그냥 통과해 뱃지가 전리품 이름으로 쌓였다.

**결과** ✅ 서로 다른 이름 **199개 → 88개**. 남은 한계: 가로로 붙은 이름(`조각난 에메랄드 조각난 사파이어`)은 아직 못 가른다. 정확한 개수도 못 센다 — `공명의 영혼석`만 뱃지가 없어 "뱃지 없음 = 1"이 성립하지 않는다.

---

## a22ee94 — 07/26 14:46 (도장 이전)

**바꾼 것** 더블 루팅을 무조건 끈다. 도전 규칙 제거.

**왜** 더블 루팅은 은동전을 20개 쓰고, 전리품이 스크롤해야 다 보인다. 스크롤 기록을 아직 못 해서 지원하면 드랍 집계가 샌다.

**결과** ✅ 켜진 채 도는 판 0건. 스크롤(`CGEvent`)이 되면 그때 옵션으로 연다.

---

## 01b76f2 — 07/26 14:24 (도장 이전)

**바꾼 것** 임무가 선택된 채로 입장하던 구멍 차단 — `enter_ready`의 금지어에 `선택됨`과 안내문 추가.

**왜** "은동전 쓰기"가 꺼져 있는데 돌자마자 은동전이 나갔다. 입장 버튼 규칙의 가드가 약해 해제 전에 눌렸다.

**결과** ✅ 해제 11회 == 입장 11회, 해제 없이 입장 0건, `enter_with_coin` 0회.

---

## 45db03c — 07/26 14:10 (도장 이전)

**바꾼 것** OCR 신뢰도 문턱 0.85 → 0.45(`clear_touch`·`reward_retry`). 150초 침묵 감시기 + 그때 화면 PNG. 클릭 박자 변주.

**왜** `화면을 터치해 주세요`에서 두 번 멈췄다. 원인은 규칙이 아니라 **신뢰도가 프레임마다 흔들리는 것** — 같은 글자가 0.5까지 떨어지는 프레임이 있다.

**결과** ✅ 같은 자리 멈춤 사라짐. 22시간 248판, 시간당 34.0판, 전투 평균 16.1초.

---

## 79f4ebc — 07/26 00:10 (도장 이전)

**바꾼 것** 화면에 떠 있는 창만 훑는다(`onScreenWindowsOnly: true`).

**왜** 캡처가 느린 이유를 두 번 짐작했고 두 번 다 틀렸다. 계측하니 시스템 창 230개를 매번 훑는 게 진짜 비용이었다.

**결과** ✅ **398ms → 269ms, 32% 단축.** 한 줄 변경. `BackgroundAutomatorProbe bench-capture --bundle-id <id>`로 언제든 다시 잰다.
