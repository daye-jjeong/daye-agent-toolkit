# Correction

같은 지시를 두 번 듣거나 "다시는 하지 마"를 들으면 `.claude/rules/correction-{YYYYMMDD}-{HHmm}-{slug}.md`에 저장한다. 1건 = 1파일. 저장 전 기존 correction·CLAUDE.md와 중복을 확인한다.

**증상이 아니라 원인을 적어라.** 사고의 마지막 표면(어느 값을 어디에 썼는지)만 금지하면 같은 원인이 다른 형태로 다시 나온다. "왜 그렇게 했나"를 한 단계 거슬러 올라가라. 원인이 안 잡히면 저장하지 말고 물어라.
